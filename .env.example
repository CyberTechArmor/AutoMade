# AutoMade Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# Application Settings
# =============================================================================
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
API_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Production Domain Settings (for Traefik + Let's Encrypt)
# DOMAIN=automade.example.com
# ACME_EMAIL=admin@example.com

# =============================================================================
# Database
# =============================================================================
DATABASE_URL=postgres://automade:automade@localhost:5432/automade

# Production: Use a strong password
# POSTGRES_PASSWORD=generate-with-openssl-rand-base64-24

# =============================================================================
# Redis
# =============================================================================
REDIS_URL=redis://localhost:6379

# Production: Use a strong password
# REDIS_PASSWORD=generate-with-openssl-rand-base64-24

# =============================================================================
# Security & Authentication
# =============================================================================
# IMPORTANT: Generate secure values for production!
# Generate with: openssl rand -base64 48
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars-for-production
JWT_ISSUER=automade
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Encryption key for sensitive data
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=generate-with-openssl-rand-hex-32

# =============================================================================
# LLM Providers (at least one required)
# =============================================================================
# Primary: Anthropic Claude
ANTHROPIC_API_KEY=your-anthropic-api-key

# Fallback 1: OpenAI GPT-4
OPENAI_API_KEY=your-openai-api-key

# Fallback 2: Google Gemini
GOOGLE_AI_API_KEY=your-google-ai-api-key

# =============================================================================
# LiveKit (Real-time Voice/Video)
# =============================================================================
# Required for voice and video discovery sessions
# Get keys from: https://cloud.livekit.io
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret
LIVEKIT_URL=wss://your-livekit-server.com

# =============================================================================
# Optional Integrations
# =============================================================================

# Twilio (Phone dial-in for sessions)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ElevenLabs (Voice synthesis for LLM responses)
# ELEVENLABS_API_KEY=your-elevenlabs-api-key
# ELEVENLABS_VOICE_ID=your-voice-id

# S3-compatible storage (for recordings, documents)
# S3_ENDPOINT=http://localhost:3902
# S3_ACCESS_KEY=your-s3-access-key
# S3_SECRET_KEY=your-s3-secret-key
# S3_BUCKET=automade
# S3_REGION=garage

# =============================================================================
# Logging
# =============================================================================
# Levels: fatal, error, warn, info, debug, trace
LOG_LEVEL=debug
